apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: workshop
  labels:
    app.kubernetes.io/name: oficina-mecanica
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: oficina-mecanica
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
